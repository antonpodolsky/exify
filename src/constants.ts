export enum ChromeBackgroundMethods {
  GET_EXIF_DATA,
}

export const OverlayExifProperties = [
  'Model',
  'FocalLength',
  'FNumber',
  'ExposureTime',
  'ISOSpeedRatings',
];

export const OverlayHeight = 60;
